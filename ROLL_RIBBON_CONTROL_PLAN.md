# [Project V] 지능형 롤리본 피딩 & 커팅 제어 시스템 설계안

본 계획서는 Epson M105 프린터 드라이버의 한계를 극복하고, 사용자가 설정한 **커팅 여분(Cutting Margin)**만큼만 리본을 정밀하게 제어하는 시스템 구축을 목표로 합니다.

## 1. 핵심 당면 과제 (Core Challenges)
*   **드라이버 강제 배출**: 윈도우 GDI 드라이버는 인쇄 종료 시 페이지 하단까지 용지를 무조건 밀어내는 성질이 있음.
*   **상태 인식 부재**: 현재 프린터에 리본이 걸려 있는지(Ready), 다 떨어졌는지(Paper Out)를 소프트웨어가 알지 못함.
*   **피딩 누적 오차**: 반복적인 여백 밀어내기 시 오차가 쌓여 인쇄 시작점이 틀어지는 현상.

## 2. 해결 전략 (Solutions)

### A. "무한 페이지(Infinite Page)" 모델 도입
*   용지 길이를 실제 리본 본체 길이보다 훨씬 길게 설정하고, 인쇄가 끝난 직후 **'작업 강제 종료'** 신호를 보내 드라이버의 자동 피딩(Form Feed)을 가로챕니다.
*   프린터는 인쇄가 끝난 지점에서 추가 피딩 없이 그 자리에 멈추게 됩니다.

### B. "스마트 피딩(Smart Feeding)" 엔진
*   **롤리본 모드**: `인쇄 구간` + `사용자 설정 커팅 여분`만큼만 정확히 좌표를 계산하여 드로잉을 종료합니다.
*   **컷리본 모드**: 인쇄 시작 전 드라이버의 `PaperSource` 속성을 강제로 `Manual` 또는 `Cut Sheet`로 전환하여 매번 용지 로딩부터 시작하게 강제합니다.

### C. 실시간 용지 상태 모니터링
*   PowerShell의 `Get-Printer` 및 `Win32_Printer` WMI 객체를 활용하여 프린터의 상태를 감시합니다.
*   `Paper Out` 감지 시 사용자에게 즉각 알림을 보내고, `Ready` 상태라면 로딩 없이 즉시 다음 인쇄를 시작합니다.

## 3. 세부 구현 로직 계획

1.  **인쇄 전 상태 체크**: 용지 유무를 WMI로 확인.
2.  **GDI 좌표 정밀 제어**: `totalLengthMM`을 인쇄가 필요한 최소 영역으로 동적 조절.
3.  **드라이버 피드 강제 중단**: 인쇄 프로세스가 완료되면 윈도우 스풀러가 페이지를 넘기기 전에 핸들을 닫아버리는 저수준 제어 시도.

---
**[사용자 승인 요청]** 위 계획대로 수정을 진행해도 괜찮을까요? 승인해 주시면 세상을 놀라게 할 인쇄 제어 코드를 작성하겠습니다!
